import { redirect } from 'next/navigation'

// Placeholder — home is served by app/page.tsx.
// Delete this file if Next.js errors on conflicting routes.
export default function MainGroupRootPage() {
  redirect('/library')
}
